# base-makefile

## Makefile.basic

simple versionon on file makefile 

## Makefile.with-sub-folder

need `*.Makefile` files

switch case on line 22 to 27 set makefile categories by makefile file name
